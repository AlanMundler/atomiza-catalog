---
import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';
import CartDrawer from '@/components/cart/CartDrawer.astro';
import perfumesData from '@/data/perfumes.json';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  children: any;
}

const { title, description = 'ATOMIZA - Decants de autor. Boutique de perfumes de lujo en decants de 2ml, 5ml y 10ml.', ogImage = '/og-image.jpg', children } = Astro.props;
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} | ATOMIZA</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(ogImage, Astro.site).href} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={new URL(ogImage, Astro.site).href} />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <Header />
    <main id="main-content" role="main">
      <slot>{children}</slot>
    </main>
    <Footer />
    <CartDrawer />
  </body>
</html>

<script>
  // Store perfumes data in localStorage for cart functionality
  if (typeof window !== 'undefined') {
    localStorage.setItem('perfumes-data', JSON.stringify({ perfumes: perfumesData.perfumes }));
  }
</script>

<style>
  body {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  main {
    flex: 1;
    padding-top: 72px; /* Header height */
  }

  @media (max-width: 640px) {
    main {
      padding-top: 64px;
    }
  }
</style>